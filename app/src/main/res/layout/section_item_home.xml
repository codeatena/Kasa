<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="10dp"
    android:id="@+id/rootview">

    <ImageView
        android:id="@+id/left_imageview"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        app:srcCompat="@drawable/bedroom_icon" />

    <TextView
        android:id="@+id/title_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@+id/left_imageview"
        android:layout_marginTop="15dp"
        android:layout_alignTop="@+id/left_imageview"
        android:text="Bedroom Lights"
        android:textColor="@color/colorMedium"
        android:textSize="@dimen/fontLarge"/>

    <TextView
        android:id="@+id/percent_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignStart="@+id/title_text"
        android:text="60%"
        android:layout_marginTop="10dp"
        android:layout_below="@+id/title_text"
        android:textColor="@color/colorWeak"
        android:textSize="@dimen/fontSmall"/>

    <net.colindodd.toggleimagebutton.ToggleImageButton
        xmlns:tib="http://schemas.android.com/apk/res-auto"
        android:id="@+id/power_imgbutton"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:background="@android:color/transparent"
        android:scaleType="fitXY"
        tib:tib_drawable_on="@drawable/power_enable_icon"
        tib:tib_drawable_off="@drawable/power_disable_icon"
         />
</RelativeLayout>